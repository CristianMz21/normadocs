[project]
name = "apascript"
version = "0.1.0"
description = "Script to convert Markdown to DOCX with APA 7th edition formatting."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-docx>=1.1.0",
]
